## 1.2
- Just like `.` stands for the current working directory, `..` stands for the parent directory. So you could do, say, `cd ../bin` if you were currently in `/usr/lib`. Ah, I guess I knew this already with `cd ..`.
- To go back to the previous directory you were in, use `cd -`
