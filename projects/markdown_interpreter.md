1. accept input as plaintext from the user using HTML form
2. parse the input using HTML, somehow detecting the formatting; using this detection, insert CSS attributes for that section (like .font-weight:bold; for * *.
3. display on the HTML page